# apps/customer-web/.env.example
# Copy to .env.local and fill in your values.

# URL of the deployed api-worker (wrangler dev runs on 8787 by default)
NEXT_PUBLIC_API_WORKER_URL=http://localhost:8787
