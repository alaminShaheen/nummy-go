/**
 * Configuration for @cloudflare/next-on-pages
 * @see https://github.com/cloudflare/next-on-pages/blob/main/packages/next-on-pages/docs/configuration.md
 */

const config = {
  // Add any custom configuration here
  // For example, to exclude certain functions from being deployed:
  // excludedFunctions: ['api/webhook'],
};

export default config;
