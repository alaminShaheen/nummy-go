{
  "name": "nummygo",
  "version": "1.0.0",
  "private": true,
  "description": "NummyGo – food-ordering monorepo (Next.js + Cloudflare Worker + Durable Objects)",
  "scripts": {
    "build:shared": "pnpm --filter @nummygo/shared build",
    "dev:api": "pnpm --filter api-worker dev",
    "dev:customer": "pnpm --filter customer-web dev:next",
    "dev:tenant": "pnpm --filter tenant-web dev:next"
  },
  "devDependencies": {
    "@types/node": "^22.15.19",
    "typescript": "^5.9.2"
  }
}
